// Arquivo gerado automaticamente. Não edite manualmente.

interface IVector3 { x: number; y: number; z: number; }
export interface IDomKeypoint { name: string; position: IVector3; }

export const mapDomKeypoints: { [key: string]: IDomKeypoint[] } = {
    "map/sandbox/summer/xml": [
        {
            "name": "A",
            "position": {
                "x": 500,
                "y": 500,
                "z": 300
            }
        }
    ]
};
